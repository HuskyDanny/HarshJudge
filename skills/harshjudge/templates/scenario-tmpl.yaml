---
id: "{scenario-id}"
slug: "{scenario-slug}"
title: "{Scenario Title}"
tags: ["{tag1}", "{tag2}"]
estimatedDuration: 30
---

# Overview

{Brief description of what this scenario tests and why it's important.}

# Prerequisites

- {Prerequisite 1: e.g., "Test user exists: test@example.com / password123"}
- {Prerequisite 2: e.g., "Application running at http://localhost:3000"}
- {Prerequisite 3: e.g., "Database seeded with test data"}

# Steps

## Step 1: {Step Title}

**Action:** {Clear description of what action to perform}

**Playwright:**
```javascript
await page.goto('{url}');
```

**Verify:** {What should be true after this action}

---

## Step 2: {Step Title}

**Action:** {Clear description of what action to perform}

**Playwright:**
```javascript
await page.fill('[data-testid="{field}"]', '{value}');
```

**Verify:** {What should be true after this action}

---

## Step 3: {Step Title}

**Action:** {Clear description of what action to perform}

**Playwright:**
```javascript
await page.click('[data-testid="{button}"]');
await page.waitForURL('{expected-url}');
```

**Verify:** {What should be true after this action}

**DB Verification:** (if applicable)
```sql
SELECT {fields} FROM {table} WHERE {condition};
-- Expected: {description of expected result}
```

# Expected Final State

- {Expected outcome 1: e.g., "User is logged in"}
- {Expected outcome 2: e.g., "Dashboard page is displayed"}
- {Expected outcome 3: e.g., "Session cookie is set"}
